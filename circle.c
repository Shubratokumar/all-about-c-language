#include <stdio.h>

int main() {
    // area of a circle
    float radius;
    printf("Enter radius : ");
    scanf("%f", &radius);

    printf("Area of the circle is: %f", 3.14 * radius * radius);
    return 0;
}